import Calendar from "./CalendarLayout";
import css from './App.module.scss';

const App = () => {
  return (
    <div className={css.wrapper}>
      <Calendar />
    </div>
  );
}

export default App;
